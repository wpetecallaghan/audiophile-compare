// Shared className fragments reused across multiple files (build step 88).
// Repeats confined to a single file get a local const there instead — see
// repeated-string-constants.md's "scope the constant to its reuse".
export const MUTED_CAPTION_CLASSES = 'text-xs text-muted'
export const SPINNER_CLASSES = 'h-6 w-6 animate-spin text-muted'
